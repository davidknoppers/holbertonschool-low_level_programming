#include <stdio.h>

/**
 * main - prints a line with printf and returns 0
 * Return: 0 in all cases
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
