#include "lists.h"
#include <stdlib.h>
#include <stdio.h>

size_t print_list(const list_t *h)
{
	unsigned int i;

	i = 0;
	while (h->str != NULL)
	{
		printf("[%d] %s\n", h->len, h->str);
		h = h->next;
		i++;
	}
	printf("[%d] %s\n", h->len, h->str);
	return (i);
}
