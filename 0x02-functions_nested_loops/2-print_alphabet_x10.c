#include "holberton.h"
/**
 * main - holds 
 * Return: the lowercase alphabet x10
 */
int main (void)
{
	int counter = 0;
	for (counter = 0; counter <= 9; counter ++)
	{
		print_alphabet();
	}
	return (0);
}
